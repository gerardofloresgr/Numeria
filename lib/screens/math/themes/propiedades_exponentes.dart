import 'package:flutter/material.dart';

class PropiedadesExponentes extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Matemáticas'),
        backgroundColor: Colors.red,
      ),
      body: Center(
        child: Text('Hola Mundo'),
      ),
    );
  }
}
